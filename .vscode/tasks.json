{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Frontend",
      "type": "npm",
      "script": "dev",
      "isBackground": true,
      "options": {
        "cwd": "${workspaceFolder}/frontend",
        "env": {
          "BACKEND_PORT": "3003"
        }
      },
      "args": ["--", "--port", "3000"],
      "problemMatcher": {
        "owner": "custom",
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": ".*(vite v|VITE v).*",
          "endsPattern": ".*ready in.*"
        }
      }
    },
    {
      "label": "Stop Frontend",
      "type": "shell",
      "command": "npx kill-port 3000 || fuser -k 3000/tcp || true",
      "presentation": {
        "reveal": "never"
      }
    }
  ]
}