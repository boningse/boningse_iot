# 温控器风速控制功能修复说明

## 问题描述

根据协议文档，温控器的风速设置应该遵循以下规范：
- `setFanSpeed` 设置风速：0=自动，1=低风，2=中风，3=高风

但是原有代码存在以下问题：
1. 前端A按钮（自动风速）发送的是字符串 `'auto'`，而不是数字 `0`
2. 后端控制器验证风速范围为1-3，不接受0值（自动风速）
3. 前端API调用参数名不匹配后端期望的参数名
4. 前端显示标签映射不正确

## 修复内容

### 1. 前端组件修复 (`/frontend/src/views/ThermostatControl.vue`)

#### 修复A按钮的值和判断逻辑
```javascript
// 修复前
:type="device.fanSpeed === 'auto' ? 'primary' : 'default'"
@click="setFanSpeed(device, 'auto')"

// 修复后
:type="device.fanSpeed === 0 ? 'primary' : 'default'"
@click="setFanSpeed(device, 0)"
```

#### 修复风速标签显示函数
```javascript
// 修复前
const getFanSpeedText = (speed) => {
  const speedMap = {
    'auto': 'A',
    1: '1',
    2: '2',
    3: '3'
  }
  return speedMap[speed] || '?'
}

const getFanSpeedLabel = (speed) => {
  const speedMap = {
    'auto': '自动',
    1: '1档',
    2: '2档', 
    3: '3档'
  }
  return speedMap[speed] || '未知'
}

// 修复后
const getFanSpeedText = (speed) => {
  const speedMap = {
    0: 'A',
    1: '1',
    2: '2',
    3: '3'
  }
  return speedMap[speed] || '?'
}

const getFanSpeedLabel = (speed) => {
  const speedMap = {
    0: '自动',
    1: '1档',
    2: '2档', 
    3: '3档'
  }
  return speedMap[speed] || '未知'
}
```

### 2. 前端API修复 (`/frontend/src/api/index.js`)

#### 修复参数名匹配
```javascript
// 修复前
setFanSpeed: (id, fanSpeed) => post(`/thermostat/devices/${id}/fan-speed`, { fanSpeed }),

// 修复后
setFanSpeed: (id, fanSpeed) => post(`/thermostat/devices/${id}/fan-speed`, { fan_speed: fanSpeed }),
```

### 3. 后端控制器修复 (`/backend/controllers/thermostatController.js`)

#### 修复风速验证逻辑
```javascript
// 修复前
if (!fan_speed || fan_speed < 1 || fan_speed > 3) {
  return res.status(400).json({
    success: false,
    message: '风速必须在1-3之间'
  });
}

// 修复后
if (fan_speed === undefined || fan_speed === null || fan_speed < 0 || fan_speed > 3) {
  return res.status(400).json({
    success: false,
    message: '风速必须在0-3之间（0为自动，1-3为档位）'
  });
}
```

## 协议对应关系

根据协议文档 `/backend/新建文本文档.txt`：

| 前端显示 | 发送值 | 协议含义 |
|---------|--------|----------|
| A按钮   | 0      | 自动风速 |
| 1按钮   | 1      | 低风     |
| 2按钮   | 2      | 中风     |
| 3按钮   | 3      | 高风     |

## 测试验证

创建了以下测试文件：
1. `/test_fan_speed_fix.js` - 后端API测试脚本
2. `/frontend/test_fan_speed.html` - 前端测试页面

### 测试步骤
1. 确保后端服务正在运行
2. 使用测试页面或脚本发送风速控制命令
3. 检查后端日志中的MQTT消息发布情况
4. 验证协议格式是否正确

### 预期MQTT消息格式
```json
{
  "uuid": "869861065084704",
  "pType": "params",
  "func": "write",
  "body": {
    "id": [1],
    "setFanSpeed": 0  // 0=自动, 1=低风, 2=中风, 3=高风
  }
}
```

## 修复效果

修复后，温控器的风速控制功能完全符合协议要求：
- ✅ A按钮正确发送0值（自动风速）
- ✅ 1、2、3按钮正确发送对应档位值
- ✅ 后端正确验证和处理所有风速值（0-3）
- ✅ MQTT消息格式符合协议规范
- ✅ 前端显示标签正确映射

所有风速档位现在都能正常工作，并且发送的MQTT命令完全符合设备协议要求。